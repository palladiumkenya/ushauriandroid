include ':app'
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT) // Allows project-specific repositories
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io'

        }
    }
}




